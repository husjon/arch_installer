# vim: ft=sh

cp ${SCRIPT_DIR}/arch-install.sh /mnt/

mkdir -p /mnt/instructions
cp -r ${SCRIPT_DIR}/instructions/chroot /mnt/instructions/

arch-chroot /mnt /arch-install.sh chroot

# cleanup
rm -r /mnt/instructions
rm /mnt/arch-install.sh
