# vim: ft=sh

hostname=laptop
domain=local

echo "${hostname}" > /etc/hostname

cat << EOF > /etc/hosts
127.0.0.1       localhost
::1             localhost
127.0.1.1       ${hostname}.${domain}   ${hostname}
EOF
